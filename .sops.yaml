creation_rules:
  - path_regex: '[^/]+\.sops\.ya?ml$'
    # ЗАМЕНИТЬ на публичный ключ той зоны, которой принадлежит проект.
    # Несколько получателей — списком через запятую в одной строке.
    age: age1REPLACE_ME_WITH_THE_ZONE_PUBLIC_KEY
